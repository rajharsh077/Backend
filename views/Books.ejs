<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Library Books</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <h1 class="text-3xl text-center font-bold mb-6">Available Books</h1>
    
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 p-4">
        <% books.forEach((book) => { %>
        <div class="bg-white shadow-md rounded-lg overflow-hidden border p-4 flex flex-col justify-between">
            <img class="w-full h-48 object-cover rounded-md" src="<%= book.image %>" alt="<%= book.title %>">
            <h2 class="text-lg font-semibold text-center mt-2"><%= book.title %></h2>
            <h4 class="text-sm text-gray-600 text-center"><%= book.author %></h4>

            <form action="/books/borrow" method="POST" class="mt-4">
                <input type="hidden" name="bookId" value="<%= book.id %>">
                <button type="submit" class="w-full bg-green-500 text-white py-2 rounded-lg hover:bg-green-600 transition">
                    Borrow
                </button>
            </form>
        </div>
        <% }); %>
    </div>
</body>
</html>

