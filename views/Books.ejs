<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Library Books</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="min-h-screen bg-gradient-to-br from-indigo-100 via-purple-100 to-pink-100 p-6">

  <h1 class="text-4xl font-bold text-center text-indigo-700 mb-10 drop-shadow-sm">
    📚 Available Books
  </h1>

  <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8 max-w-7xl mx-auto">
    <% books.forEach((book) => { %>
    <div class="bg-white rounded-2xl shadow-xl hover:shadow-2xl transition duration-300 ease-in-out overflow-hidden flex flex-col justify-between border border-gray-100">
      <img src="<%= book.image %>" alt="<%= book.title %>" class="w-full h-48 object-cover"/>

      <div class="p-4 text-center">
        <h2 class="text-xl font-semibold text-indigo-800"><%= book.title %></h2>
        <p class="text-gray-500 text-sm mt-1">by <%= book.author %></p>
      </div>

      <form action="/books/borrow" method="POST" class="p-4 pt-0">
        <input type="hidden" name="bookId" value="<%= book.id %>"/>
        <button type="submit"
                class="w-full py-2 mt-2 bg-gradient-to-r from-green-500 to-emerald-600 text-white font-semibold rounded-lg hover:scale-[1.03] hover:shadow-md transition-all">
          Borrow
        </button>
      </form>
    </div>
    <% }); %>
  </div>

</body>
</html>
